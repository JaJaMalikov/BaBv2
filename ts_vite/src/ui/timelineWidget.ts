export class TimelineWidget {
  mount(_element: HTMLElement) {
    // UI setup placeholder
  }

  addKeyframeMarker(_frame: number) {}
  removeKeyframeMarker(_frame: number) {}
  clearKeyframes() {}
  setCurrentFrame(_frame: number) {}
}
